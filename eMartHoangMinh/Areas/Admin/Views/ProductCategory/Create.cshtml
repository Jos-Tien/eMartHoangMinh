@model eMartHoangMinh.Models.FE.ProductCategory
@{
    ViewBag.Title = "Tạo mới danh mục sản phẩm";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

@section leftNavbar{
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="#" class="nav-link">Trang chủ</a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="/admin/productcategory" class="nav-link active">Danh mục sản phẩm</a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="#" class="nav-link active">Tạo mới danh mục sản phẩm</a>
        </li>
    </ul>
}

@*header*@
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>Tạo mới danh mục sản phẩm</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="/admin/home">Trang chủ</a></li>
                    <li class="breadcrumb-item"><a href="/admin/productcategory">Danh mục sản phậm</a></li>
                    <li class="breadcrumb-item active">Thêm mới</li>
                </ol>
            </div>
        </div>
    </div>
</section>

@*main*@
<section class="content">
    <div class="card card-primary">
        <div class="card-header">
            <h3 class="card-title">Thông tin danh mục sản phẩm</h3>
        </div>
        <div class="card-body">
            @using (Html.BeginForm("Create", "ProductCategory", FormMethod.Post, new { }))
            {
                @Html.AntiForgeryToken()
                @Html.ValidationSummary(true)
                <div class="bs-stepper-content">
                    <div class="form-group">
                        <label for="Name">Tiêu đề</label>
                        @Html.TextBoxFor(x => x.Name, new { @class = "form-control", @placeholder = "Tiêu đề danh mục", @type = "text", @id = "inputName" })
                        @Html.ValidationMessageFor(x => x.Name, null, new { @class = "text-danger" })
                    </div>
                    <div class="form-group">
                        <label for="Description">Mô tả</label>
                        @Html.TextAreaFor(x => x.Description, new { @class = "form-control", @placeholder = "Chi tiết", @id = "inputDescription", @rows = "4" })
                    </div>
                    <div class="form-group">
                        <label for="SeoName">Tên SEO</label>
                        @Html.TextBoxFor(x => x.SeoName, new { @class = "form-control", @placeholder = "Tiêu đề SEO", @type = "text", @id = "inputSeoName" })
                    </div>
                    <div class="form-group">
                        <label for="SeoDescription">Mô tả SEO</label>
                        @Html.TextAreaFor(x => x.SeoDescription, new { @class = "form-control", @placeholder = "Chi tiết từ khóa SEO", @id = "inputSeoDescription", @rows = "4" })
                    </div>
                    <div class="form-group">
                        <label for="SeoKeywords">Seo Keywords</label>
                        @Html.TextAreaFor(x => x.SeoKeywords, new { @class = "form-control", @placeholder = "Từ khóa SEO", @id = "inputKeywords", @rows = "4" })
                    </div>
                    <div class="form-group">
                        <label for="Position">Vị trí hiển thị</label>
                        @Html.TextBoxFor(x => x.Position, new { @class = "form-control", @value = 1, @type = "number", @id = "inputSpentBudget" })
                    </div>
                    <div class="form-group">
                        <a href="#" class="btn btn-secondary">Hủy</a>
                        <input type="submit" formaction="Create" value="Lưu" class="btn btn-success">
                    </div>
                </div>
            }

        </div>
    </div>

</section>

